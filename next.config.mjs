/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com", "s11.gifyu.com"], // Use the base domain only
  },
};

export default nextConfig;
